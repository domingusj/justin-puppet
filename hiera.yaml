---
:backends:
  - yaml

:yaml:
  :datadir: "/etc/puppetlabs/code/environments/%{::environment}/hieradata"

:hierarchy:
  - "node/%{::clientcert}"
  - "%{::location}/%{::env}.%{::location}"
  - "env/%{::env}"
  - "%{::location}/%{::location}"
  - common
