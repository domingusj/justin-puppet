---
:backends:
  - yaml

:yaml:
  :datadir: /vagrant/hieradata

:hierarchy:
  - "nodes/%{::clientcert}"
  #- "%{::location}/%{::env}.%{::location}"
  #- "env/%{::env}"
  #- "%{::location}/%{::location}"
  - common
